server:
  port: ${port:8080}

spring:
  data:
    mongodb:
      uri: ${mongo:}
      database: ${database:dnser}

cloudflare:
  url: https://api.cloudflare.com/client/v4/
  zone: ${zone:}
  token: ${token:}