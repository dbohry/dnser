spring:
  data:
    mongodb:
      uri: ${MONGO:}
      database: ${DATABASE:dnser-test}

cloudflare:
  url: https://api.cloudflare.com/client/v4/
  zone: ${CLOUDFLARE_ZONE:}
  token: ${CLOUDFLARE_TOKEN:}